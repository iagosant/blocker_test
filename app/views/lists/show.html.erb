<p id="notice"><%= notice %></p>

<p><strong>Name:</strong><%= @list.name %></p>

<p><strong>Description:</strong><%= @list.description %></p>

<%= link_to 'Edit', edit_list_path(@list) %> | <%= link_to 'Back', lists_path %>

<h4 class="list_name"><%= @list.name %></h4>
<p class="list_sub_title"><%= @list.description %></p>

<div id="task_wrapper">
  <%= render "tasks/form" %>
 	<%= render @list.tasks %>
</div>

<div class="links">
	<%= link_to 'Edit', edit_list_path(@list) %> |
	<%= link_to 'Delete', list_path(@list), method: :delete, data: { confirm: "Are you sure?" }
 %> |
  <%= link_to 'Back', lists_path %>
</div>
